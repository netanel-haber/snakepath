# Tests to skip ONLY on POSIX systems (Linux, macOS)
# Format: module.class.method (one per line)
# Lines starting with # are comments
#
# Note: WindowsPathTest is automatically skipped by CPython on POSIX systems.
# PosixPath* tests moved to skip_common.txt (fail on both platforms).

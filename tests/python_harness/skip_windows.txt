# Tests to skip ONLY on Windows systems
# Format: module.class.method (one per line)
# Lines starting with # are comments
#
# These tests require POSIX-specific features or can't run on Windows.
# Note: PosixPath cannot be instantiated on Windows, so all PosixPath* tests
# will fail at class instantiation time.

# Currently empty - PosixPath tests are auto-skipped by CPython on Windows,
# and concrete filesystem tests are in skip_common.txt.
#
# Add Windows-specific skips here as needed, for example:
# - Tests that pass on POSIX but fail on Windows due to path semantics
# - Tests that require POSIX-only features not available on Windows
